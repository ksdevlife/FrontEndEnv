var assert = require('assert');
var functional = require('../src/functional');

describe('Array#join', () => {
  it('joins all elements into a string with separator', () => {
    assert(['a', 'b', 'c'].join(':') === 'a:b:c');
  });
});

describe('Create#new', () => {
  it('', () => {


  });
});